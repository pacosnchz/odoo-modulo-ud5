<odoo>
  <data>

    <!-- Vista lista -->
    <record model="ir.ui.view" id="escuela.profesor_list">
      <field name="name">profesor list</field>
      <field name="model">escuela.profesor</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
        </tree>
      </field>
    </record>

    <!-- Vista formulario -->
    <record model="ir.ui.view" id="escuela.profesor_form">
      <field name="name">profesor form</field>
      <field name="model">escuela.profesor</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group>
              <field name="name"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Acción -->
    <record model="ir.actions.act_window" id="escuela.action_profesor">
      <field name="name">Profesores</field>
      <field name="res_model">escuela.profesor</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- Menú -->
    <menuitem name="Escuela" id="escuela.menu_root"/>
    <menuitem name="Profesores" id="escuela.menu_profesores"
              parent="escuela.menu_root"
              action="escuela.action_profesor"/>

  </data>
</odoo>